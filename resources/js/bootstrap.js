// This is the bootstrap JavaScript, meaning it has to be loaded before anything else

// JQuery
window.$ = window.jQuery = require('jquery')

// Animate on Scroll
import AOS from 'aos';
import 'aos/dist/aos.css';
AOS.init();

// Font Awesome Icons